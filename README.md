# Omniscripta

## TODO

- Add a dedicated section that documents everything the app creates under `data/`: the directory structure, the contents of each subtree, who produces it, who consumes it, and what each path means.
